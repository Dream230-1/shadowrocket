meta:
  version: 3.1.1
  generated_date: '2026-07-29'
  project_path: LOWERTOP-Enterprise-v3.1
  design:
    - RC2 验证内核兼容层
    - RC1 Performance 行为契约冻结
    - AI / Apple / 通信 / 媒体 / 游戏 / 广告 / 基础路由模块化
    - Apple Global 与 Apple Core 负向回归
    - 真实离线与在线审计链
    - 全局规则冲突审计
    - ETag / Last-Modified 条件缓存
    - Wi-Fi / 蜂窝 / 网络切换设备验证模板
    - 外部 reject_list 与 Ultra+ 分层去广告
    - Direct / Modular 可复现发布
    - 核心配置不内置通用去广告规则或 MITM
    - iCloud 专用代理分流
    - DNS 泄漏防护
    - Apple 天气增强（QWeather，本地密钥）
    - Script / Rule / MITM 分段结构校验
    - 公开仓库凭证泄漏防护
    - Developer Toolkit
    - 规则精简去重

routing_overrides:
  # v3.1.1 删除旧 iCloud、Telegram 与 YouTube select。
  # 专用代理 mask 端点由更靠前的 Apple-Global-AI 精确规则交给 AI。
  # Telegram 与 YouTube 规则统一交给已实机验证稳定的 AI fallback。
  remove_proxy_groups:
    - iCloud
    - Telegram
    - YouTube
  policy_aliases:
    Telegram: AI
    YouTube: AI
  local_ruleset_policies:
    Apple-iCloud: DIRECT
