# 可选脚本注入配置
# script_rules、extra_rules 与 mitm_hostname 必须由生成器分别写入正确配置段。
# v3.1 禁止向公开仓库写入 API Key、Token 或私钥。
#
# Apple 天气增强位于 modules/ 下，不自动注入主配置，以便单独启停、验证和回滚。

script_rules: []

mitm_hostname: []

# v3.1.1 不在核心配置中重复通用广告规则。
# 域名级拦截由外部 reject_list 负责，响应改写由独立模块负责。
extra_rules: []
