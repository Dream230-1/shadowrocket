name: LOWERTOP Enterprise v3.1.1 Build and Audit

on:
  push:
    paths:
      - "LOWERTOP-Enterprise-v3.1/**"
      - "LOWERTOP-Enterprise-v3.0-RC3/**"
      - ".github/workflows/lowertop-enterprise-v31.yml"
  pull_request:
    paths:
      - "LOWERTOP-Enterprise-v3.1/**"
      - "LOWERTOP-Enterprise-v3.0-RC3/**"
      - ".github/workflows/lowertop-enterprise-v31.yml"
  workflow_dispatch:
  schedule:
    - cron: "37 3 * * 1"

permissions:
  contents: read

env:
  PROJECT_DIR: LOWERTOP-Enterprise-v3.1

concurrency:
  group: lowertop-v31-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  offline-build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: ${{ env.PROJECT_DIR }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: ${{ env.PROJECT_DIR }}/requirements.txt
      - run: pip install -r requirements.txt
      - run: python scripts/ci.py
      - if: always()
        uses: actions/upload-artifact@v7
        with:
          name: lowertop-enterprise-v3.1.1-offline
          if-no-files-found: warn
          path: |
            ${{ env.PROJECT_DIR }}/build/**
            ${{ env.PROJECT_DIR }}/modular/**
            ${{ env.PROJECT_DIR }}/reports/**
            ${{ env.PROJECT_DIR }}/experimental/**
            ${{ env.PROJECT_DIR }}/RELEASE-VALIDATION.md

  online-audit:
    needs: offline-build
    runs-on: ubuntu-latest
    timeout-minutes: 40
    defaults:
      run:
        working-directory: ${{ env.PROJECT_DIR }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: ${{ env.PROJECT_DIR }}/requirements.txt
      - name: Restore remote-rule conditional cache
        uses: actions/cache@v5
        with:
          path: ${{ env.PROJECT_DIR }}/.cache
          key: lowertop-v31-rules-${{ runner.os }}-${{ hashFiles('LOWERTOP-Enterprise-v3.1/config/release.yaml', 'LOWERTOP-Enterprise-v3.0-RC3/manifest.yaml') }}
          restore-keys: |
            lowertop-v31-rules-${{ runner.os }}-
      - run: pip install -r requirements.txt
      - run: python scripts/ci.py --online
      - if: always()
        uses: actions/upload-artifact@v7
        with:
          name: lowertop-enterprise-v3.1.1-online
          if-no-files-found: warn
          path: |
            ${{ env.PROJECT_DIR }}/build/**
            ${{ env.PROJECT_DIR }}/modular/**
            ${{ env.PROJECT_DIR }}/reports/**
            ${{ env.PROJECT_DIR }}/experimental/**
            ${{ env.PROJECT_DIR }}/RELEASE-VALIDATION.md
